{
  "name": "Basic app",
  "description": "A simple Sinatra application that adds some hacks around",
  "repository": "https://github.com/fathersoil/soil-test",
  "success_url": "/",
  "env": {
    "TWITTER_CONSUMER_KEY": {
      "description": "Your Twitter Application API Key from https://apps.twitter.com",
      "required": true
    },
    "TWITTER_CONSUMER_SECRET": {
      "description": "Your Twitter Application API Secret from https://apps.twitter.com",
      "required": true
    },
    "TWITTER_SEARCH_STRING": {
      "description": "Your Twitter Search Terms (`-rt` means without retweets)",
      "value": "#tedc15 -rt",
      "required": false
    },
    "MIN_IMG_INDEX": {
      "description": "Minimum image index",
      "value": 0,
      "required": true
    },
    "MAX_IMG_INDEX: {
      "description": "Maximum image index",
      "value": 5,
      "required": true
    }
  }
}
